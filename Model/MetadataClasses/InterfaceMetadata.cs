using System;
using System.Collections.Generic;
using System.Text;

namespace Model.MetadataClasses
{
    class InterfaceMetadata : TypeMetadata
    {
        InterfaceMetadata(Type type) : base(type)
        {

        }
    }
}
